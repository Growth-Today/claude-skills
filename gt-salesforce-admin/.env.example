# Copy to a `.env` in the skill root (or the playbook folder you run).
# Salesforce username + password + security token auth (or use a connected app).
SF_USERNAME=user@company.com
SF_PASSWORD=your-password
SF_SECURITY_TOKEN=your-security-token
SF_DOMAIN=login    # use "test" for a sandbox
